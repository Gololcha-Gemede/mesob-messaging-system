# TODO

## Letter preview
- [x] Rich HTML preview restored (`LetterRenderer` default mode: `rich`)
- [x] Plain mode still available via `mode="plain"` when needed

## Direct message
- [x] `direct_message` is a letter template (Compose dropdown + server/client formatters)
- [x] Separate DM chat UI removed; use Compose with Direct Message template
