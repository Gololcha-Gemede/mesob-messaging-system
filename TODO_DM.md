# Direct messages

Direct messages are **not** a separate app section. Use **Compose → Letter template → Direct Message** and send like any other correspondence.

Legacy `/api/dm` routes and `DMInboxPage` / `DMChatPage` are unused (API unmounted from `server.js`).
