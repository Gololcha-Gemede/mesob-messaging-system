# Database (required)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=imms_db

# Auth (required)
JWT_SECRET=change_this_to_a_long_random_string

# Server
PORT=5000

# Set to true only if you want open self-registration at POST /api/auth/register
ALLOW_PUBLIC_REGISTER=false
